<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="e.par.connectingmist_30">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Connecting MIST"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<service-->
            <!---->
            <!--android:label="@string/app_name">-->
            <!--<intent-filter>-->
                <!--<action android:name="your.app.domain.NotificationService" />-->

                <!--<category android:name="android.intent.category.DEFAULT" />-->
            <!--</intent-filter>-->
        <!--</service>-->
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Newsfeed_Notice.NoticeActivity" />
        <activity android:name=".ClubActivity" />
        <activity android:name=".Newsfeed_Notice.NewsfeedActivity" />
        <activity android:name=".Newsfeed_Notice.NewsActivity" />
        <activity android:name=".Club_activity.ClubDetailActivity" />
        <activity android:name=".Club_activity.AddInfo" />
        <activity android:name=".Newsfeed_Notice.Edit_NewsfeedActivity" />
        <activity android:name=".Newsfeed_Notice.Edit_NoticeActivity" />
        <activity android:name=".MyprofileActivity" />
        <activity android:name=".UpdateActivity"/>
        <activity android:name=".ProviderPicUpload"/>
        <activity android:name=".Calender_view.Notice_cal" />


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCp-qWaetbQoaXH0b3w2MmiTyTzk9Gq85g" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".AdminMenuActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".AtaglanceActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".HistoryActivity"></activity>
    </application>

</manifest>